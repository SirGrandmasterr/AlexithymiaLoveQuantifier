<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately empty. RECORD_AUDIO is declared in the app's own manifest
  (android-config/app/src/main/AndroidManifest.xml, CHANGE 5) so that every permission the
  APK asks for is readable in one file, with its reasoning beside it.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
